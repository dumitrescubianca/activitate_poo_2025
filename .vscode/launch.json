{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Rulează (clang++, LLDB)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${fileDirname}/a.out",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${fileDirname}",
      "MIMode": "lldb",
      "externalConsole": true,
      "preLaunchTask": "C/C++: clang build active file"
    }
  ]
}
